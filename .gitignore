# Dependencies
node_modules

# Build output
dist
dist-ssr

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment variables (NEVER commit secrets)
.env
.env.*
!.env.example

# Lock files (keep only one - using package-lock.json for npm)
bun.lockb

# Python virtual environment
.venv/

# Temp / working files
.tmp/
_cleanup_*/

# Generated content & large media (should use cloud storage or LFS)
execution/generated_carousels/
public/remotion/out/
_bmad-output/implementation-artifacts/test-artifacts/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test output
playwright-report
test-results
*.local
.playwright-cli/

# Playwright auth sessions (contain real tokens — NEVER commit)
playwright/auth-sessions/

# Sensitive / project-specific
unipile-node-sdk
cacao-ai-07483356a7e4.json

# MCP server configs (contain API tokens — NEVER commit)
mcp_servers.json
.mcp.json

# Parallel agent logs
logs/parallel-agents/
.aider*
